<h1> Show Department</h1>
	<h3>Department Name:<%= @department.name %></h3>
	<%= link_to'Back to all department list', departments_path %> | 
	<%= link_to'Edit', edit_department_path %> |
	<%= link_to'Delete', department_path(@department), method: :delete, data: {confirm: 'Are you sure to delete?'}%>


